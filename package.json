{
  "name": "camel_game",
  "version": "1.0.0",
  "description": "Cardboard desert scene served from src directory",
  "scripts": {
    "start": "npx serve src",
    "build": "rm -rf dist && cp -r src dist",
    "test": "vitest"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "jsdom": "^24.0.0",
    "vitest": "^1.5.0"
  }
}
